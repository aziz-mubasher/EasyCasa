import { SellerListingWizard } from '@/components/seller/SellerListingWizard';

/** Private-seller T07 wizard (PR-W). Agency users keep /add. */
export default function SellerListPage() {
  return <SellerListingWizard />;
}
