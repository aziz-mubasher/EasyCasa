'use client';

import { SearchBar } from '@/components/search/SearchBar';

/** Compact search bar for the homepage hero — navigates to /search on submit. */
export function HeroSearchBar() {
  return <SearchBar compact />;
}
