/**
 * TypeScript entry for platform-specific MapView.{native,web}.tsx.
 * Metro resolves the platform file at bundle time; this stub is for `tsc`.
 */
import type { MapProps } from './Map';

export default function EasyCasaMap(_props: MapProps): null {
  return null;
}
