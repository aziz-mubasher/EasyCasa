# Minimal valid env for the CI boot check (Phase 33).
# ALLOW_PROVIDER_STUBS=true + EC_TEST_AUTH=true so OIDC is not required; seam vars left empty (fail-soft).
NODE_ENV=test
ALLOW_PROVIDER_STUBS=true
EC_TEST_AUTH=true
API_PORT=4000
DATABASE_URL=postgresql://easycasa:easycasa@127.0.0.1:5432/easycasa_test
MEILI_URL=http://127.0.0.1:7700
MEILI_MASTER_KEY=test
PHONE_OTP_PEPPER=test-phone-otp-pepper-xx
