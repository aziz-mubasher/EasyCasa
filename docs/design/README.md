# Design references

## `EC_CRM_Admin_Prototype_v1.html`

Canonical filename for the K EC 4.1 CRM clickable mockup (UTF-8, commit as-is).

**v1.1 note:** Claude’s attached binary was **not present** on the EasyCasa cloud-agent filesystem for this send. The file currently at this path (and the duplicate `EC_CRM_Admin_Prototype_v1.cursor-authored.html`) is Cursor’s self-authored reference from the v1.0 round. When the authentic Claude HTML is available, replace `EC_CRM_Admin_Prototype_v1.html` with that file unchanged and keep the `.cursor-authored` copy for delta comparison.
