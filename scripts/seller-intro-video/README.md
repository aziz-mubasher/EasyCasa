# Private-seller intro video

Regenerate the Italian film (voice + slides):

```bash
node scripts/seller-intro-video/generate.mjs
```

Writes `apps/web/public/videos/vendi-da-privato-intro.it.{mp4,vtt}` and the poster.

Copy and legal notes: `docs/marketing/seller-intro-video.md`.
Scene list: `scenes.json`.
